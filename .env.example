# Server-only secret used by api/chat.js — never prefix this with VITE_,
# or it would be inlined into the client bundle and publicly exposed.
# Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key-here
